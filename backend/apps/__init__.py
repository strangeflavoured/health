"""Django applications for the health data backend.

Contains the api app (Redis reads and REST endpoints) and the workers
app (Celery tasks for background computation, to be expanded with
PyTorch inference).
"""
