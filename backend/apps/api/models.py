"""Database models for the API app.

Currently empty — data is stored in Redis rather than the relational
database. Add models here if persistent relational data is needed, for
example to store user-specific configuration or computation results.
"""
